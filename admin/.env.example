# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8787

# Authentication (change these passwords in production!)
# Admin has full access (view + edit)
NEXT_PUBLIC_ADMIN_PASSWORD=admin123
# Viewer has read-only access (view only)
NEXT_PUBLIC_VIEWER_PASSWORD=viewer123
